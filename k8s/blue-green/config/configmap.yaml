apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: blue-green-deployment
data:
  ENV: "production"          # quotes optional here but good habit
  LOG_LEVEL: "info"
  DATABASE_URL: "your-database-url"
  FEATURE_FLAG: "true"       # ← must be string "true" (or "false")